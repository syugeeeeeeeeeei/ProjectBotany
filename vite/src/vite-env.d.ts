/**
 * Vite 環境変数型定義 (vite-env.d.ts)
 *
 * 【動機】
 * Vite 特有の機能（クライアント側の型定義や、`import.meta.env` など）を
 * TypeScript コンパイラに認識させるためです。
 *
 * 【恩恵】
 * - 画像や CSS などのアセットのインポート、および環境変数に対する
 *   適切な型補完と静的チェックを提供します。
 *
 * 【使用法】
 * 通常、手動で編集することはありません。`vite` パッケージによって管理されます。
 */
/// <reference types="vite/client" />
