/**
 * 🌿 Field Grid Feature (フィールド・グリッド表示機能)
 * 
 * 【動機】
 * 7x10 のゲーム盤面（フィールド）を 3D 空間に構築し、各マスの状態（生態系）を
 * 視覚的に表現するためです。
 *
 * 【恩恵】
 * - 盤面全体を 3D オブジェクトとして管理し、一括で回転やスケーリングを行うことができます。
 * - 各マスの座標計算を一元化し、他機能からの参照（クリック位置の特定など）を容易にします。
 *
 * 【使用法】
 * `App.tsx` 内で `GameBoard3D` を配置し、`useGameStore` から取得した `gameField` データを渡します。
 */

export const initFieldGrid = () => {
	// UI機能のみのため、ロジックの初期化は不要
};

export { default as GameBoard3D } from "./ui/GameBoard3D";
