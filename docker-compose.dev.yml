# docker-compose.dev.yml (Development overrides)
services:
  vite:
    container_name: vite_dev
    ports:
      - "5173:5173"
    volumes:
      - ./vite:/app:delegated
      - /app/node_modules
    command: sh -c "yarn dev"