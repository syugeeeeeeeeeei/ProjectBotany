# docker-compose.yml (Base configuration)
services:
  vite:
    build:
      context: ./vite
      dockerfile: Dockerfile
    networks:
      - project_botany_network

networks:
  project_botany_network:
    driver: bridge