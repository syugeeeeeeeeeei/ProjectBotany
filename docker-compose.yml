# docker-compose.yml (Base configuration)
services:
  vite:
    build:
      context: ./vite
      dockerfile: Dockerfile
    networks:
      - app_network

networks:
  app_network:
    driver: bridge