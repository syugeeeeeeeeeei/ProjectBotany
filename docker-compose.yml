name: projectbotany
services:
  # -----------------------------------------------------------
  #  🌿 ProjectBotany (Production / Base)
  # -----------------------------------------------------------
  projectbotany-vite:
    build:
      context: ./vite
      dockerfile: Dockerfile
      target: prod # 本番用ステージを使用
    container_name: projectbotany-vite
    restart: unless-stopped
    # 本番ではボリュームマウントを行わない (COPY済みのため)
    labels:
      caddy: http://projectbotany.app.fukaya-sus.lab
      caddy.reverse_proxy: "{{upstreams 80}}" # Nginxの80番ポートへ

networks:
  fukaya-lab-network:
    external: true
    name: fukaya-lab-network